# Bu dosyayı kopyalayıp `.env` yapın:  copy .env.example .env  (PowerShell: Copy-Item .env.example .env)
# Prisma ve (isteğe bağlı) yerel veritabanı için gerekli:

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/doctor_clinic?schema=public"

# Vercel: Project → Settings → Environment Variables → DATABASE_URL (üretim Postgres bağlantınız)
